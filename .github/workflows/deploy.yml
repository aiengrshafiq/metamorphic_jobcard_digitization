name: Build and Deploy to Azure Container Apps

on:
  push:
    branches:
      - main  # Trigger this workflow on every push to the main branch

env:
  RESOURCE_GROUP: 'jobcard_rg' 
  LOCATION: 'uaenorth'
  ACR_NAME: 'jobcardacr'
  CONTAINER_APP_ENV: 'jobcard-env'
  CONTAINER_APP_NAME: 'jobcard-app'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: 'Az CLI login'
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: 'Build and push image'
      uses: azure/docker-login@v1
      with:
        login-server: ${{ env.ACR_NAME }}.azurecr.io
        username: ${{ secrets.ACR_USERNAME }}
        password: ${{ secrets.ACR_PASSWORD }}
    - run: |
        docker build . -t ${{ env.ACR_NAME }}.azurecr.io/metamorphic-job-card:${{ github.sha }}
        docker push ${{ env.ACR_NAME }}.azurecr.io/metamorphic-job-card:${{ github.sha }}

    - name: 'Deploy to Azure Container Apps'
      uses: azure/container-apps-deploy@v2
      with:
        resourceGroup: ${{ env.RESOURCE_GROUP }}
        containerAppName: ${{ env.CONTAINER_APP_NAME }}
        imageToDeploy: ${{ env.ACR_NAME }}.azurecr.io/metamorphic-job-card:${{ github.sha }}
        
        # Step 1: Securely pass GitHub secrets to create secrets in Azure.
        # Format is: 'azure-secret-name ${{ secrets.github-secret-name }}'
        secrets: |
          database-url ${{ secrets.DATABASE_URL }}
          secret-key ${{ secrets.SECRET_KEY }}
          azure-storage-connection-string ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          openai-api-key ${{ secrets.OPENAI_API_KEY }}
        
        # Step 2: Tell the container to use those Azure secrets as its environment variables.
        # Format is: 'ENV_VAR_NAME=secretref:azure-secret-name'
        environmentVariables: |
          DATABASE_URL=secretref:database-url
          SECRET_KEY=secretref:secret-key
          AZURE_STORAGE_CONNECTION_STRING=secretref:azure-storage-connection-string
          OPENAI_API_KEY=secretref:openai-api-key